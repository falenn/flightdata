#Spring

# Kafka
# ${spring.emb}
kafka:
  broker:
    boostrap-servers: localhost:9092
  etl-subflow-example:
    producer:
      topic: subflow.in
    consumer:
      auto-offset-reset: earliest
      group-id: etl-subflow-example
      topic: subflow.out
    